name: Qodana
permissions: read-all
on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [develop, master, qodana, default, qodana-2]

jobs:
  qodana:
    runs-on: self-hosted # Targets your macOS runner
    permissions:
      contents: write
      pull-requests: write
      checks: write
    env:
      GRADLE_OPTS: "-Dfile.encoding=UTF-8"

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Prepare SSH key
        run: |
          mkdir -p .ssh
          echo "${{ secrets.SUBMODULE_SSH_KEY}}" > .ssh/id_rsa
          chmod 600 .ssh/id_rsa

      # Run Qodana using Docker Desktop's built-in macOS SSH bridge
      - name: 'Qodana Scan'
        uses: JetBrains/qodana-action@v2025.3
        with:
          use-nightly: true
          # Bind mount the special macOS Docker Desktop SSH bridge
          args: |
            --image,registry.jetbrains.team/p/sa/containers/qodana-jvm:2025.3-nightly,
            -e,GIT_SSH_COMMAND=ssh -i /root/.ssh/id_rsa -o StrictHostKeyChecking=no,
            -v,${{ github.workspace }}/.ssh:/root/.ssh
          upload-result: true
          pr-mode: 'true'
        env:
          QODANA_TOKEN: ${{ secrets.QODANA_TOKEN }}
